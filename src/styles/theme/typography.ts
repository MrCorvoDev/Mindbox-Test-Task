const SIZES = {
   default: 16,
   pc: 20,
   mobile: 16,
};

const FONTS = {
   family1: 'Unbounded, serif',
};

const typography = {
   sizes: SIZES,
   fonts: FONTS,
};

export default typography;
